# multithreaded-socket-server
A multi-user chat application written in C using the Client/Server model. It features concurrent client handling via POSIX threads and implements strict synchronization (Mutex) to safely log chat history and manage active users without race conditions.
